/**
 * @typedef {Object} MCP
 * @property {string}  id          - Unique identifier for the MCP
 * @property {string}  name        - Display name of the MCP
 * @property {string}  description - A brief description of the MCP
 * @property {string}  version     - Version of the MCP
 * @property {boolean} enabled     - Whether the MCP is currently active
 */

/**
 * Represents the configuration structure, which is an array of MCP objects.
 * @typedef {MCP[]} McpConfig
 */

// This file primarily serves for JSDoc type definitions.
// No actual code execution is expected from it.
module.exports = {}; // Or export const {} if using ES modules in future, but for JSDoc, this is fine.
// --- End of src/types/mcp.js
